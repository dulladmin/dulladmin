/* Code generated by DullAdmin; DO NOT EDIT. */

import type { RouteRecordRaw } from 'vue-router';

const menuTree: RouteRecordRaw[] = [
{{#each menu.items}}
  {{#if children}}
  {
    name: '{{name}}',
    path: '{{path}}',
    children: [
    {{#each children}}
      {
        name: '{{name}}',
        path: '{{path}}',
        component: () => import('{{viewImportPath}}'),
        meta: {
          roles: ['*'],
          {{#if icon}}
          icon: '{{icon}}',
          {{/if}}
          title: '{{title.i18nKey}}',
        },
      },
    {{/each}}
    ],
    meta: {
      {{#if icon}}
      icon: '{{icon}}',
      {{/if}}
      title: '{{title.i18nKey}}',
    },
  },
  {{else}}
  {
    name: '{{name}}',
    path: '{{path}}',
    component: () => import('{{viewImportPath}}'),
    meta: {
      roles: ['*'],
      {{#if icon}}
      icon: '{{icon}}',
      {{/if}}
      title: '{{title.i18nKey}}',
    },
  },
  {{/if}}
{{/each}}
];

export default menuTree;
